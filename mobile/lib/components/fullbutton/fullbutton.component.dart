import 'package:flutter/material.dart';

class FullbuttonComponent extends StatelessWidget {
  const FullbuttonComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: BoxDecoration(
        
      ),
    );
  }
}